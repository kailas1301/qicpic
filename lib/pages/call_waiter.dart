import 'package:flutter/material.dart';

class CallWaiterPage extends StatelessWidget {
  const CallWaiterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Call Waiter'),
      ),
      body: Center(child: Text('Call Waiter Page')),
    );
  }
}
